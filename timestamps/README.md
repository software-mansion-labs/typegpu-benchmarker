Here lie the debug timestamps

We do timestamps at the beginning of the script (`origin`), before and after each example, the last timestamp is captured after saving results to file. Results saved to file are traslated by `-origin` value.
